import { NavbarClient, type NavbarCategory } from "./NavbarClient";

export function Navbar({ categories, cartCount }: { categories: NavbarCategory[]; cartCount?: number }) {
  return <NavbarClient categories={categories} cartCount={cartCount} />;
}

